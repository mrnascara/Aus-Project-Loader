#include "raylib.h"
//makey makey extension stuff,note:will add combos later

ifkey_down('down')
{
     if (IsKeyDown(KEY_DOWN))
       
}
ifkey_down('left')
{
     if (IsKeyDown(KEY_LEFT))
       
}
ifkey_down('up')
{
     if (IsKeyDown(KEY_UP))
       
}
ifkey_down('right')
{
     if (IsKeyDown(KEY_RIGHT))
       
}
